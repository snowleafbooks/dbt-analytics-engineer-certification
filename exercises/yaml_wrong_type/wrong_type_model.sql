-- Stub body. It exists only so the YAML above has a node to attach to: a properties
-- block with no matching model is a warning ("Did not find matching node for patch"),
-- not an error, and the type check never runs.
select 1 as order_id
